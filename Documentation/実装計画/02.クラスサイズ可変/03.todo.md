# クラスサイズ可変 ToDo

## フェーズ1: モデルのサイズ正規化

- [x] `src/models/diagramModel.js` に `CLASS_MIN_SIZE` を追加する。
- [x] `DiagramModel.normalizeClassSize(size)` を追加する。
- [x] `normalizeState()` の `classNode.size` 補完処理を `normalizeClassSize()` に置き換える。
- [x] `createClass()` の既定 `size` を `normalizeClassSize()` 経由にする。
- [x] 古い保存データや Mermaid インポート結果で `size` が欠けても、幅・高さが補完されることを確認する。

## フェーズ2: SVG 表示サイズ計算の統一

- [x] `src/views/svgRenderer.js` に `getClassRenderSize(classNode)` を追加する。
- [x] `renderClassNode()` の幅・高さ計算を `getClassRenderSize()` に置き換える。
- [x] `classBounds()` を `height` 引数ではなく `size` 引数で判定する形に変更する。
- [x] 範囲選択の交差判定で `getClassRenderSize()` を使う。
- [x] `centerOf()` と `edgePoint()` を表示サイズ引数を受け取る形に変更する。
- [x] `relationshipPath()` で接続線端点の計算に表示サイズを使う。

## フェーズ3: リサイズハンドル描画

- [x] `SvgRenderer.renderResizeHandles(classNode, size)` を追加する。
- [x] 単一クラス選択時のみ、右端・下端・右下角のハンドルを描画する。
- [x] ハンドルに `pointerdown` を設定し、`startResizePointer()` を呼び出す。
- [ ] 複数クラス選択時にハンドルが表示されないことを確認する。

## フェーズ4: リサイズドラッグ処理

- [x] `SvgRenderer.startResizePointer(event, classNode, direction)` を追加する。
- [x] `handlePointerMove()` に `drag.type === "resize"` の分岐を追加する。
- [x] ドラッグ方向 `east`、`south`、`south-east` に応じて幅・高さを更新する。
- [x] 最小サイズ未満にならないように `CLASS_MIN_SIZE` で制限する。
- [x] `endPointer()` に `resize` 終了分岐を追加する。
- [ ] リサイズハンドル操作がクラス移動として扱われないことを確認する。

## フェーズ5: コントローラー連携と保存

- [x] `DiagramController.bindViews()` に `onClassResize` と `onClassResizeEnd` を追加する。
- [x] `DiagramController.updateClassSize(classId, size)` を追加する。
- [x] `DiagramController.finishClassResize(classId, moved)` を追加する。
- [x] リサイズ中は `shouldSave=false`、リサイズ終了時のみ保存する。
- [ ] リサイズ終了後も対象クラスの選択状態が維持されることを確認する。

## フェーズ6: スタイル追加

- [x] `src/styles.css` に `.resize-handle` の基本スタイルを追加する。
- [x] `.resize-handle.east` に `ew-resize` カーソルを設定する。
- [x] `.resize-handle.south` に `ns-resize` カーソルを設定する。
- [x] `.resize-handle.south-east` に `nwse-resize` カーソルを設定する。
- [x] ホバー時の見た目を追加する。

## フェーズ7: 動作確認

- [ ] クラス選択時にリサイズハンドルが表示されることを確認する。
- [ ] 右端ドラッグで幅のみ変更できることを確認する。
- [ ] 下端ドラッグで高さのみ変更できることを確認する。
- [ ] 右下角ドラッグで幅と高さを同時に変更できることを確認する。
- [ ] 最小サイズ未満に縮まらないことを確認する。
- [ ] 関係線がリサイズ後の外周に追従することを確認する。
- [ ] リサイズ後にページを再読み込みし、サイズが復元されることを確認する。
- [x] Mermaid エクスポートにサイズ情報が出力されないことを確認する。
- [ ] Mermaid インポート後のクラスをリサイズできることを確認する。
- [x] Mermaid インポート直後に、長いプロパティ名・メソッド名に合わせてクラス幅が自動調整されることを確認する。
- [x] Mermaid インポート直後に、プロパティ数・メソッド数に合わせてクラス高さが自動調整されることを確認する。
- [x] Mermaid インポート直後に、自動調整されたクラス同士が初期配置で重なりにくいことを確認する。
- [ ] クラス移動、範囲選択、パン、ズーム、関係選択が退行していないことを確認する。
