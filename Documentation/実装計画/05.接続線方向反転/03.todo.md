# 接続線方向反転 ToDo

- [x] `DiagramModel.reverseRelationship` を追加し、両端のクラス ID と多重度を交換する。
- [x] `InspectorView` の関係編集フォームへ「方向を反転」ボタンを追加する。
- [x] `DiagramController` で反転イベントを既存の `updateState` に接続する。
- [x] モデル反転のユニットテストを追加する。
- [x] Mermaid の出力・再インポートで反転状態を保持するテストを追加する。
- [x] 自動テストを実行する。
- [ ] ブラウザで関係種別ごとの表示、保存、再読み込みを手動確認する。
