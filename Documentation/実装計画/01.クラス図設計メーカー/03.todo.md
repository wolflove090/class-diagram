# クラス図設計メーカー ToDo

## フェーズ1: 静的アプリ基盤

- [x] `index.html` を作成する。
- [x] `src/styles.css` を作成する。
- [x] `src/main.js` を作成する。
- [x] `index.html` を直接開ける通常の script 読み込み構成にする。
- [x] ツールバー、SVG ビュワー、インスペクター、モーダル領域を画面に配置する。

## フェーズ2: 内部データモデル

- [x] `src/models/diagramModel.js` を作成する。
- [x] `DiagramState` の構造を定義する。
- [x] クラスノード生成処理を実装する。
- [x] プロパティ生成処理を実装する。
- [x] メソッド生成処理を実装する。
- [x] パラメータ生成処理を実装する。
- [x] 関係生成処理を実装する。
- [x] クラス削除時に関連する関係も削除する処理を実装する。
- [x] 接続元・接続先が存在しない関係を検出する検証処理を実装する。

## フェーズ3: SVG 描画

- [x] `src/views/svgRenderer.js` を作成する。
- [x] SVG の `defs` に関係種別ごとの marker を定義する。
- [x] クラスノードを `g`、`rect`、`text`、区切り線で描画する。
- [x] プロパティ一覧をクラスノード内に描画する。
- [x] メソッド一覧をクラスノード内に描画する。
- [x] 接続線をクラスノード間に描画する。
- [x] 接続線のクリック判定用 hit path を描画する。
- [x] 選択状態のスタイルを描画に反映する。

## フェーズ4: コントローラーと UI 操作

- [x] `src/controllers/diagramController.js` を作成する。
- [x] ツールバーのボタンイベントを実装する。
- [x] クラス追加操作を実装する。
- [x] 選択操作を実装する。
- [x] 削除操作を実装する。
- [x] クラスノードのドラッグ移動を実装する。
- [x] ドラッグ範囲選択で複数クラスを選択できるようにする。
- [x] 選択した複数クラスをまとめてドラッグ移動できるようにする。
- [x] ビュワー全体のパン操作を実装する。
- [x] ズームイン・ズームアウトを実装する。
- [x] 表示位置リセットを実装する。
- [x] 接続モードとクラス間接続作成を実装する。

## フェーズ5: インスペクター

- [x] `src/views/inspectorView.js` を作成する。
- [x] 未選択状態の表示を実装する。
- [x] クラス選択時の編集フォームを実装する。
- [x] クラス名、種別、ステレオタイプ、可視性の編集を実装する。
- [x] プロパティの追加、編集、削除、並び替えを実装する。
- [x] メソッドの追加、編集、削除、並び替えを実装する。
- [x] メソッドパラメータの追加、編集、削除を実装する。
- [x] 関係選択時の編集フォームを実装する。
- [x] 関係種別、ラベル、多重度の編集を実装する。

## フェーズ6: LocalStorage 自動保存

- [x] `src/services/storageService.js` を作成する。
- [x] アプリ起動時の復元処理を実装する。
- [x] 編集後の自動保存処理を実装する。
- [x] 保存データ破損時のエラー処理を実装する。
- [x] 現在の編集状態をクリアする処理を実装する。

## フェーズ7: Mermaid エクスポート

- [x] `src/services/mermaidSerializer.js` を作成する。
- [x] `classDiagram` ヘッダーを出力する。
- [x] クラス定義を出力する。
- [x] プロパティを Mermaid 形式へ変換する。
- [x] メソッドを Mermaid 形式へ変換する。
- [x] 継承、実装、依存、関連、集約、コンポジションを Mermaid 関係記法へ変換する。
- [x] Mermaid 出力モーダルを実装する。
- [x] 出力テキストをコピーできるようにする。

## フェーズ8: Mermaid インポート

- [x] `src/services/mermaidParser.js` を作成する。
- [x] `classDiagram` 入力を認識する。
- [x] クラスブロックを解析する。
- [x] 単行メンバー定義を解析する。
- [x] 関係行を解析する。
- [x] 日本語名を内部データへ保持する。
- [x] 読み取れない行を破棄する。
- [x] クラス図情報が全く読み取れない場合のエラーを実装する。
- [x] `src/services/layoutService.js` を作成する。
- [x] インポート後の自動初期配置を実装する。
- [x] インポート成功時に現在の図を置き換える。

## フェーズ9: 検証と調整

- [x] ローカルブラウザで起動確認する。
- [x] クラス追加、編集、削除を確認する。
- [x] プロパティとメソッドの追加、編集、削除を確認する。
- [x] 接続線の作成、選択、削除を確認する。
- [x] クラス削除時に接続線も削除されることを確認する。
- [x] パン、ズーム、表示リセットを確認する。
- [x] LocalStorage の保存・復元を確認する。
- [x] Mermaid エクスポートを確認する。
- [x] Mermaid インポートを確認する。
- [x] 不正入力時のエラー表示を確認する。
- [x] 日本語名の作成、保存、復元を確認する。
- [x] トラックパッドの通常スクロールをズームではなくパン操作として扱う。
- [x] トラックパッドのピンチズームはズーム操作として維持する。

## フェーズ10: Undo

- [x] ツールバーに Undo ボタンを追加する。
- [x] 図の編集操作を履歴に積む処理を実装する。
- [x] クラス移動とリサイズは確定時に1操作として履歴化する。
- [x] `Ctrl+Z` / `Cmd+Z` で Undo できるようにする。
- [x] Undo 後の状態を LocalStorage に保存する。
