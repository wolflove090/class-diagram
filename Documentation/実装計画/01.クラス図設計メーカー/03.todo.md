# クラス図設計メーカー ToDo

## フェーズ1: 静的アプリ基盤

- [ ] `index.html` を作成する。
- [ ] `src/styles.css` を作成する。
- [ ] `src/main.js` を作成する。
- [ ] ES Modules で各 JavaScript ファイルを読み込める構成にする。
- [ ] ツールバー、SVG ビュワー、インスペクター、モーダル領域を画面に配置する。

## フェーズ2: 内部データモデル

- [ ] `src/models/diagramModel.js` を作成する。
- [ ] `DiagramState` の構造を定義する。
- [ ] クラスノード生成処理を実装する。
- [ ] プロパティ生成処理を実装する。
- [ ] メソッド生成処理を実装する。
- [ ] パラメータ生成処理を実装する。
- [ ] 関係生成処理を実装する。
- [ ] クラス削除時に関連する関係も削除する処理を実装する。
- [ ] 接続元・接続先が存在しない関係を検出する検証処理を実装する。

## フェーズ3: SVG 描画

- [ ] `src/views/svgRenderer.js` を作成する。
- [ ] SVG の `defs` に関係種別ごとの marker を定義する。
- [ ] クラスノードを `g`、`rect`、`text`、区切り線で描画する。
- [ ] プロパティ一覧をクラスノード内に描画する。
- [ ] メソッド一覧をクラスノード内に描画する。
- [ ] 接続線をクラスノード間に描画する。
- [ ] 接続線のクリック判定用 hit path を描画する。
- [ ] 選択状態のスタイルを描画に反映する。

## フェーズ4: コントローラーと UI 操作

- [ ] `src/controllers/diagramController.js` を作成する。
- [ ] ツールバーのボタンイベントを実装する。
- [ ] クラス追加操作を実装する。
- [ ] 選択操作を実装する。
- [ ] 削除操作を実装する。
- [ ] クラスノードのドラッグ移動を実装する。
- [ ] ビュワー全体のパン操作を実装する。
- [ ] ズームイン・ズームアウトを実装する。
- [ ] 表示位置リセットを実装する。
- [ ] 接続モードとクラス間接続作成を実装する。

## フェーズ5: インスペクター

- [ ] `src/views/inspectorView.js` を作成する。
- [ ] 未選択状態の表示を実装する。
- [ ] クラス選択時の編集フォームを実装する。
- [ ] クラス名、種別、ステレオタイプ、可視性の編集を実装する。
- [ ] プロパティの追加、編集、削除、並び替えを実装する。
- [ ] メソッドの追加、編集、削除、並び替えを実装する。
- [ ] メソッドパラメータの追加、編集、削除を実装する。
- [ ] 関係選択時の編集フォームを実装する。
- [ ] 関係種別、ラベル、多重度の編集を実装する。

## フェーズ6: LocalStorage 自動保存

- [ ] `src/services/storageService.js` を作成する。
- [ ] アプリ起動時の復元処理を実装する。
- [ ] 編集後の自動保存処理を実装する。
- [ ] 保存データ破損時のエラー処理を実装する。
- [ ] 現在の編集状態をクリアする処理を実装する。

## フェーズ7: Mermaid エクスポート

- [ ] `src/services/mermaidSerializer.js` を作成する。
- [ ] `classDiagram` ヘッダーを出力する。
- [ ] クラス定義を出力する。
- [ ] プロパティを Mermaid 形式へ変換する。
- [ ] メソッドを Mermaid 形式へ変換する。
- [ ] 継承、実装、依存、関連、集約、コンポジションを Mermaid 関係記法へ変換する。
- [ ] Mermaid 出力モーダルを実装する。
- [ ] 出力テキストをコピーできるようにする。

## フェーズ8: Mermaid インポート

- [ ] `src/services/mermaidParser.js` を作成する。
- [ ] `classDiagram` 入力を認識する。
- [ ] クラスブロックを解析する。
- [ ] 単行メンバー定義を解析する。
- [ ] 関係行を解析する。
- [ ] 日本語名を内部データへ保持する。
- [ ] 読み取れない行を破棄する。
- [ ] クラス図情報が全く読み取れない場合のエラーを実装する。
- [ ] `src/services/layoutService.js` を作成する。
- [ ] インポート後の自動初期配置を実装する。
- [ ] インポート成功時に現在の図を置き換える。

## フェーズ9: 検証と調整

- [ ] ローカルブラウザで起動確認する。
- [ ] クラス追加、編集、削除を確認する。
- [ ] プロパティとメソッドの追加、編集、削除を確認する。
- [ ] 接続線の作成、選択、削除を確認する。
- [ ] クラス削除時に接続線も削除されることを確認する。
- [ ] パン、ズーム、表示リセットを確認する。
- [ ] LocalStorage の保存・復元を確認する。
- [ ] Mermaid エクスポートを確認する。
- [ ] Mermaid インポートを確認する。
- [ ] 不正入力時のエラー表示を確認する。
- [ ] 日本語名の作成、保存、復元を確認する。
